# 2026-08 remediation-queue register

Fixes landed by working the hand-ordered
[remediation queue](../../../docs/findings/REMEDIATION-QUEUE.md) established on
2026-08-05 from the [revalidation register](../2026-08-revalidation/README.md).
One entry per closed finding; each archived report records the resolution
commit, regression coverage, and canonical architecture link.

## Fixed findings

| Finding | Resolution | Coverage |
|---|---|---|
| [Live model-job claim precedes chat-row durability](fixed/model-job-claim-precedes-chat-durability.md) | this remediation commit (2026-08-07) — successful publication, not raw EOF, unlocks a forced exact-row committed-save barrier; live ownership defers concurrent recovery, typed main-job connection loss retains artifacts without changing aux retry/fallback, failed resends keep dual identity, and generation-conditional deletion cannot erase a replacement send | `liveModelJobFinalization.test.ts`, `liveModelJobSend.test.ts`, `modelJobDisposition.test.ts`, `jobFetch.test.ts`, `jobRecovery.test.ts`, `pendingSends.test.ts`, `chatPersistStage.test.ts`, `model-jobs.test.ts` (forced checkpoint bypass/order, non-commit and publication-failure retention, typed main loss/ordinary aux retry policy, resend identity, default/proxy/aux behavior, recursion/discovery races, cross-generation replacement, cleanup failures) |
| [CharX importer mistakes JSON assets for metadata](fixed/charx-importer-mistakes-json-assets-for-metadata.md) | this remediation commit (2026-08-07) — normalized path/schema-based `x_meta/` handling preserves raw asset keys and card-reference aliases while normalized collisions and malformed or unknown metadata fail closed | `processzip.test.ts` (direct and nested-package JSON/non-media assets, escaping/dot-prefixed paths, referenced `x_meta/` asset override, collision and malformed/unknown metadata rejection) |
| [CCv2 export drops regex lore semantics](fixed/ccv2-export-drops-regex-lore-semantics.md) | this remediation commit (2026-08-07) — one exhaustive CCv2/CCv3 lore-entry adapter serializes explicit `use_regex` booleans without changing activation or matching behavior | `characterCards.loreRoundTrip.test.ts` (public CCv2 JSON/PNG semantic export/import round trips for regex `true` and literal `false`, valid trimmed PNG preservation, and CCv3 shared-adapter guard) |
| [Imported card triggers can bulk-delete chat history without the low-level-access consent](fixed/card-triggers-can-bulk-delete-history-without-consent.md) | this remediation commit — independent destructive owner consent and runtime gating; durable-target publication queues forced+required `script-bulk-chat` pre-images for full and delta saves without granting unrelated low-level APIs | `scriptCapabilities.test.ts`, `triggers.destructiveAccess.test.ts`, `scriptings.destructiveAccess.test.ts`, `chatSendTarget.test.ts`, `nodeStorageAvailability.test.ts`, `chatBackups.test.ts`, `chat-content-row.test.ts` (import consent, V1/V2/Lua gates, target publication, reason propagation, cooldown bypass, exact recovery, fail-closed capture) |
| [Inlay filename mapping is not injective](fixed/inlay-filename-mapping-is-not-injective.md) | this remediation commit — versioned bounded lowercase-hex paths within the historical UTF-8 tuple envelope; injective dotted-extension archive names; strict storage/direct-asset transport identity; complete physical accounting; exact-entry collision rejection; and deterministic exact-ID legacy resolution/canonicalization | `inlay-filename-mapping.test.ts`, `inlay-publication-atomicity.test.ts`, `inlay-kv-backup-fallback.test.ts`, `backupEntryFormat.test.ts` (dotted/reserved/exact-limit/multibyte IDs, over-limit no-side-effect rejection, archive-tuple and transport aliases, sidecar-only operations, prefix/deletion aliases, list/backup/import/migration, staged failures, crash-orphan accounting, compression, process-kill durability, KV fallback) |
| [External dedup can strand or overwrite a live asset](fixed/external-dedup-can-strand-or-overwrite-a-live-asset.md) | this remediation commit — canonical save-level cross-process maintenance locks span every live mutation and import swap lifecycle; controlled byte-verified link-to-temp plus final inode/content revalidation, atomic rename, directory fsync, and interrupted-temp recovery replace external pathname mutation | `assetMaintenanceLock.test.ts`, `assetStore.test.ts`, `assetDedup.test.ts` (true cross-process exclusion/stale-owner and symlink identity policy, owner-publication and import-settlement cleanup faults, target/shipping validation, hidden temp recovery/exclusion, runtime-write and import-swap races, detachment, post-validation replacement, SIGKILL across dedup pathname publication) |
| [Runtime asset writes can replace a case-colliding asset on case-insensitive volumes](fixed/runtime-asset-writes-can-case-fold-clobber.md) | this remediation commit — centralized portable runtime eligibility and exact directory-entry identity; buffered/spooled collisions and non-portable names stay KV-authoritative without case-aliased read shadowing | `assetStore.test.ts`, `admitted-write-spool.test.ts` (simulated case-insensitive exact-name read/metadata/delete and collision checks; buffered/spooled route backend placement, both payloads, reserved/stale-file KV precedence, ordinary/direct reads) |
| [Inlays still served from KV fallback are omitted from backups and cleared on restore](fixed/unmigrated-kv-inlays-are-omitted-from-backups.md) | this remediation commit — pinned filesystem/KV component union, filesystem precedence without stale duplication, explicit unsafe-ID failure, and retryable completion-marker accounting | `inlay-kv-backup-fallback.test.ts` (full/server/main export and restore, info preservation, FS precedence, post-cut source race, post-marker save-folder rows, marker retry, unsafe-ID diagnostics, upstream/partial omission) |
| [Chat-version backups do not keep referenced inlays live](fixed/chat-version-backups-do-not-keep-referenced-inlays-live.md) | this remediation commit — combined live-row and strict federated retained-history reachability with queued fail-closed deletion revalidation | `chatBackups.test.ts`, `inlay-reference-guard.test.ts` (loose/gzip/frame/legacy-bundle and active/historical/protected-conflict roots, unavailable roots and conflict containers, unreadable directories, malformed metadata, root and inventory/read disappearance, delete-chat/pre-image protection, race revalidation, true orphans, restore and direct media readability) |
| [Changing the chat-backup root hides all existing version history](fixed/changing-chat-backup-root-hides-all-existing-version-history.md) | this remediation commit — complete chat/server marker-root federation, independent create-only direct/protected copies, non-destructive retained-source federation and normalization, decoded-history permanence, and safe stable same-ID aliases | `chatBackups.test.ts`, `backup-roundtrip.test.ts` (A→B→default authenticated visibility, frame/loose/gzip/bundle conflicts, decoded dedup, destination/source in-place replacement races, non-destructive bundle retry and protected frame derivation, final-boundary replacement with stable restart aliases, reserved-subtree exclusion, peer-temp ownership, ctime and decode-to-cache identity races, file/parent fsync and normalization failures, restart/reorder/offline cases, retained legacy server roots, retention isolation, marker preservation) |
| [Best-effort path markers let updaters delete recovery directories](fixed/best-effort-path-markers-let-updaters-delete-recovery-directories.md) | this remediation commit — atomic/fsynced multi-root markers, recoverable startup quarantine, process-local + filesystem exclusion with Windows handoff, canonical/lexical identity policy, case-correct preservation, and fail-closed updater preflight | `recoveryPathMarkers.test.ts`, `portableUpdaterRecovery.test.ts`, `updateScript.test.ts`, `windowsRecoveryLockFinalizer.test.ts`, `backup-roundtrip.test.ts` (crash/concurrency/cross-process/startup/post-step gates, first/second marker and quarantine faults, clean-restart historical-root recovery, real successful updater replacements, inaccessible/alias/read failures, mixed-case preservation) |
| [Queued plugin-recovery actions are not bound to the writer epoch](fixed/queued-recovery-actions-ignore-writer-epoch.md) | `3d7e7fb6` in-queue writer revalidation + epoch-bound recovery tokens | `test/compat/plugin-storage-boot-reconcile.test.ts` (stale-epoch reject, happy path, queued-takeover race) |
| [Serve pushes and releases are not gated by the test suites](fixed/compatibility-suites-not-run-in-ci.md) | `f519771d` reusable tests.yml gate on serve pushes + releases, compat skip guard; flake hardening `ee924ac6` | The gate itself; skip guard exercised on pass/unexpected/empty reports |
| [Reroll can leave no durable copy of the discarded response](fixed/reroll-discards-the-only-copy-within-preimage-cooldown.md) | `a772b134` destructive reasons force required pre-images; consume-on-success client reasons; delete-swipe tagged | `test/compat/chat-content-row.test.ts` (cooldown-exempt capture + byte-exact recovery, failure abort), `chatBackups.test.ts`, `chatStorage.test.ts` |
| [Chat-row staging is not bound to the committed stub snapshot](fixed/chat-row-stage-is-not-bound-to-the-committed-stub-snapshot.md) | `73d5c87e` rediscovery loop + synchronous dispatch guard; orphan sweep captures required pre-images | `chatPersistStage.test.ts` (paused-row creation, non-convergence), `db-chunking.test.ts` (swept-orphan restore, capture-failure skip) |
| [Whole-chat patches can partially commit external rows](fixed/whole-chat-patches-partially-commit-rows.md) | `6e6725e2` definitive 422 rejection before any mutation; non-atomic extraction path removed | `database-write-atomicity.test.ts` (two-payload zero-commit, overwrite untouched, stub-only unaffected), `chatRows.test.ts` |
| [Asset filename mapping is not portable across filesystems](fixed/portable-asset-filename-mapping.md) | `f3e8aa11` host-independent portable identity; migration collision preflight leaves colliding/non-portable rows in SQLite; imports demote staged case-collisions to SQLite instead of aborting; runtime-write residual extracted to a new open finding | `assetStore.test.ts` (predicates, preflight matrix, simulated case-folding fs), `backup-roundtrip.test.ts` (seeded migration retention, lossless colliding import) |
| [Upstream-target export can emit a PocketRisu-only magic version byte](fixed/upstream-export-plugin-proto-header.md) | `3da789b4` upstream target reuses the main post-spool header guard: definitive 409 `BACKUP_UPSTREAM_UNSUPPORTED_PLUGIN_KEYS` before archive publication; UI surfaces the migration message | `plugin-backup-entries.test.ts` (escape-key upstream export rejects with no archive headers; pinned upstream 7/8/9 + `RISUSAVE\0` header acceptance on ordinary exports) |
| [Acknowledged database patches are not durable for up to five seconds](fixed/acknowledged-patches-are-not-durable.md) | `e526398e` structural patches commit before acknowledging; fatal handlers run a guarded emergency flush; client stages non-durable acks behind a durable `flushDatabase()` ETag confirmation with replay | `test/compat/staged-patch-durability.test.ts` (SIGKILL row reachability, staged/flush semantics), `server/node/dbCachePersistence.test.ts`, `src/ts/storage/stagedAckTracker.test.ts`, `test/compat/database-write-atomicity.test.ts` |
| [The save loop stops retrying after five consecutive failures](fixed/save-loop-idles-after-five-failures.md) | `5e329fb6` `SaveRetryScheduler`: quick backoff for four failures, then alert once per streak + 10 s one-shot watchdog cadence with online expedite and displaced-writer guard | `src/ts/storage/saveRetryScheduler.test.ts` (quick/slow protocol, one-shot wake, online expedite, success reset, five-failure recovery scenario) |
| [Non-optimized plugin save storage acknowledges before persistence](fixed/nonoptimized-save-storage-acks-before-persistence.md) | `67f9eb96` inline set/remove/batch/viewer/clear mutations gate acknowledgement on a committed `requireDurable` save via a coalescing durability ticket; non-committed flushes throw `PLUGIN_STORAGE_INLINE_DURABILITY` as `unknown` without rolling back memory; no-op and pre-`saveDb` boot-window writes stay staged by design | `src/ts/plugins/pluginSaveStorage.test.ts` "inline plugin storage durability" (gated ack, failure-outcome mapping in both modes incl. outcome APIs, no-op/boot-window exemptions, coalescing, optimized-path divergence guard) |
| [Bulk filesystem writes can commit a partial prefix](fixed/bulk-filesystem-writes-partially-commit.md) | `64042a36` ordered per-entry durable outcomes with authoritative reconciliation; duplicate/reserved-row preflight; queue-time legacy-hash revalidation; bounded strict client acknowledgements | `test/compat/bulk-asset-write-outcomes.test.ts` (publication/commit/metadata/reconciliation faults, restart, duplicates, reserved rows, mixed routing, queue race, retry), `nodeStorageAvailability.test.ts`, `backup-roundtrip.test.ts` |
| [Boot spool sweep can unlink another instance's active file](fixed/boot-spool-sweep-can-unlink-another-instances-active-file.md) | `f4432368` pre-child canonical-save-root claim plus private installation-owned child; lock-free identity convergence, pinned old/fresh quarantine cleanup, retained conflicts, and process-lifetime pinned runtime access preserve peers and path replacements | `spoolOwnership.test.ts` (no-follow/type/mode/claim/inert-lock/fsync/pre-rename/post-pin/post-close/conflict/runtime-swap matrix), `snapshot-spool.test.ts` (first-boot and concurrent copied-owner claims, active spool publication, symlink-victim survival, own orphan cleanup, default/custom roots), `admitted-write-spool.test.ts`, `snapshotPluginStorage.e2e.test.ts`, `importListBarrier.e2e.test.ts` |
| [Decoded stream-load spools bypass the configured spool and orphan sweep](fixed/decoded-stream-load-spools-bypass-configured-spool-and-orphan-sweep.md) | `98b3e2a7` shared private decoded-name families in the required configured owned spool; fail-closed production readiness and owned boot sweep; normal cleanup precedes acknowledgement | `streamRisuLoadTopLevel.test.ts` (file-source routing, mode, traversal cleanup), `snapshot-spool.test.ts` (default/custom SIGKILL recovery, all decoded families, unavailable-spool refusal), `stream-risu-load.test.ts` (bounds/abort/corruption/legacy cleanup), `plugin-storage-mutation-atomicity.test.ts` (pre-ack spool cleanup) |
| [Wall-clock rollback disables chat pre-image capture](fixed/wall-clock-rollback-disables-chat-preimage-capture.md) | `c537c131` cooldown only for non-negative elapsed wall-clock time; successful rollback recovery capture establishes the new same-process baseline | `server/node/chatBackups.test.ts` (in-process rollback, restart with a future disk timestamp, byte-exact recovery history, immediate/cooldown-minus-one/exact-expiry boundaries) |
